Rounak Das
SE20UCSE149
CSE3

input.txt should be in the same directory as the program. Run the C++ program (SE20UCSE149.cpp) to get the output in stdout. To get an output in a file named output.txt, just uncomment Line 91. 

The approach used here is depth first search, similar to that of graph coloring problem. Every sequence is generated as per availability and need and then checked whether it is safe or not.